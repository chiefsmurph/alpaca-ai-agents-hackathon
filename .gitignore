# This is a curated public showcase — keep it that way.
# Never commit secrets, real env, or the private agent's data.
.env
.env.*
*.key
*.pem
# Ignore any REAL journal, but keep the obviously-synthetic sample committed.
decisions.jsonl
data/*.jsonl
!data/decisions.sample.jsonl
node_modules/
dist/
*.log
.DS_Store
